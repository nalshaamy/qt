Cairo local font files
======================

Place these licensed WOFF2 files in this folder using the exact names:

- Cairo-Regular.woff2
- Cairo-SemiBold.woff2
- Cairo-Bold.woff2

The module is already configured to load them locally.
Until the files are added, the UI safely falls back to Tahoma without breaking the layout.
