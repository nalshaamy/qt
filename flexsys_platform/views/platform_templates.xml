<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="command_palette" name="FlexSys Command Palette">
        <button type="button" class="fl-command-trigger" data-fl-command-open="1" aria-label="Open command palette">
            <span class="fa fa-search"/><span>Search or run a command</span><kbd>Ctrl K</kbd>
        </button>
        <div class="fl-command-backdrop" data-fl-command-backdrop="1" hidden="hidden">
            <section class="fl-command-dialog" role="dialog" aria-modal="true" aria-label="FlexSys command palette">
                <header class="fl-command-search">
                    <span class="fa fa-search" aria-hidden="true"/>
                    <input type="search" data-fl-command-input="1" maxlength="100" autocomplete="off" placeholder="Search applications, orders, customers..." aria-label="Search commands"/>
                    <kbd>Esc</kbd>
                </header>
                <div class="fl-command-results" data-fl-command-results="1" role="listbox"/>
                <footer class="fl-command-footer"><span><kbd>↑</kbd><kbd>↓</kbd> Navigate</span><span><kbd>Enter</kbd> Open</span></footer>
            </section>
        </div>
    </template>

    <template id="login_page" name="FlexSys Platform Login">
        <t t-call="web.layout">
            <div class="fl-platform-page fl-login-page">
                <div class="fl-login-card">
                    <div class="fl-brand">
                        <div class="fl-brand-mark">FS</div>
                        <div><h1>FlexSys</h1><p>Smart Operations Platform</p></div>
                    </div>
                    <form method="post" action="/flexsys/login">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <label>Username</label>
                        <input name="login" type="text" autocomplete="username" required="required" autofocus="autofocus"/>
                        <label>Password</label>
                        <input name="password" type="password" autocomplete="current-password" required="required"/>
                        <div t-if="error" class="fl-alert fl-alert-danger"><span class="fa fa-exclamation-circle"/><t t-esc="error"/></div>
                        <button type="submit" class="fl-btn fl-btn-primary">Sign in</button>
                    </form>
                </div>
            </div>
        </t>
    </template>

    <template id="launcher_page" name="FlexSys Mission Control">
        <t t-call="web.layout">
            <div class="fl-platform-page fl-launcher">
                <div class="fl-shell">
                    <header class="fl-launcher-header">
                        <div class="fl-brand">
                            <div class="fl-brand-mark">FS</div>
                            <div><h1>FlexSys</h1><p>Smart Operations Platform</p></div>
                        </div>
                        <div class="fl-user-context">
                            <span class="fl-user-pill" t-esc="platform_user.name"/>
                            <form method="post" action="/flexsys/context" class="fl-context-form">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                <select name="company_id" aria-label="Company" onchange="this.form.submit()">
                                    <option t-foreach="allowed_companies" t-as="item"
                                            t-att-value="item.id" t-att-selected="item == company"
                                            t-esc="item.name"/>
                                </select>
                                <select name="branch_id" aria-label="Branch" onchange="this.form.submit()">
                                    <option value="">All branches</option>
                                    <option t-foreach="allowed_branches" t-as="item"
                                            t-att-value="item.id" t-att-selected="item == branch"
                                            t-esc="item.name"/>
                                </select>
                            </form>
                            <a href="/flexsys/logout" class="fl-btn fl-btn-ghost"><span class="fa fa-sign-out"/>Sign out</a>
                        </div>
                    </header>
                    <t t-call="flexsys_platform.command_palette"/>

                    <form action="/flexsys/search" method="get" class="fl-global-search" role="search">
                        <span class="fa fa-search" aria-hidden="true"/>
                        <input type="search" name="q" minlength="2" maxlength="100" placeholder="Search orders, customers and more..." aria-label="Search FlexSys"/>
                        <button type="submit" class="fl-btn fl-btn-primary">Search</button>
                    </form>

                    <main>
                        <section class="fl-welcome">
                            <div class="fl-welcome-content">
                                <span class="fl-eyebrow"><span class="fa fa-compass"/> Mission Control</span>
                                <h2>Welcome, <t t-esc="platform_user.name"/></h2>
                                <p>
                                    <t t-esc="company.name"/>
                                    <t t-if="branch"> · <t t-esc="branch.name"/></t>
                                    <t t-else=""> · All branches</t>
                                </p>
                            </div>
                            <div class="fl-health-summary">
                                <span t-attf-class="fl-status-chip fl-status-#{platform_health}">
                                    <span class="fl-status-dot"/><t t-esc="platform_health.title()"/>
                                </span>
                                <strong>Platform status</strong>
                            </div>
                        </section>

                        <section class="fl-kpi-grid" aria-label="Platform overview">
                            <article class="fl-kpi-card">
                                <span class="fl-kpi-icon fa fa-th-large"/>
                                <div><span>Applications</span><strong t-esc="len(applications)"/></div>
                            </article>
                            <article class="fl-kpi-card">
                                <span class="fl-kpi-icon fa fa-check-circle"/>
                                <div><span>Healthy</span><strong t-esc="health_counts['healthy']"/></div>
                            </article>
                            <article class="fl-kpi-card">
                                <span class="fl-kpi-icon fa fa-exclamation-triangle"/>
                                <div><span>Needs attention</span><strong t-esc="health_counts['degraded'] + health_counts['unavailable']"/></div>
                            </article>
                            <article class="fl-kpi-card">
                                <span class="fl-kpi-icon fa fa-users"/>
                                <div><span>Active sessions</span><strong t-esc="active_sessions"/></div>
                            </article>
                        </section>

                        <section>
                            <div class="fl-section-head">
                                <div><h2>Workspaces</h2><p>Only applications available to your role are shown.</p></div>
                            </div>
                            <div class="fl-app-grid">
                                <a t-foreach="applications" t-as="app" t-att-href="'/flexsys/open/%s' % app.code" class="fl-app-card">
                                    <span t-attf-class="fa #{app.icon} fl-app-icon"/>
                                    <div class="fl-app-content">
                                        <div class="fl-app-title-row">
                                            <h3 t-esc="app.name"/>
                                            <span t-attf-class="fl-status-chip fl-status-#{app.health_status}">
                                                <span class="fl-status-dot"/><t t-esc="health_labels.get(app.health_status)"/>
                                            </span>
                                        </div>
                                        <p t-esc="app.summary"/>
                                        <span class="fl-app-meta">Version <t t-esc="app.version"/></span>
                                    </div>
                                    <span class="fa fa-arrow-right fl-app-arrow"/>
                                </a>
                                <div t-if="not applications" class="fl-empty-state">
                                    <span class="fa fa-th-large"/>
                                    <h3>No workspaces available</h3>
                                    <p>Ask an administrator to assign an application permission to your account.</p>
                                </div>
                            </div>
                        </section>

                        <section class="fl-activity-section">
                            <div class="fl-section-head">
                                <div><h2>Recent activity</h2><p>Latest platform events in the current context.</p></div>
                            </div>
                            <div class="fl-panel">
                                <div t-if="recent_logs" class="fl-activity-list">
                                    <article t-foreach="recent_logs" t-as="log" class="fl-activity-item">
                                        <span t-attf-class="fl-activity-icon fa #{'fa-shield' if log.event_type == 'security' else 'fa-sign-in' if log.event_type == 'authentication' else 'fa-bolt'}"/>
                                        <div class="fl-activity-content">
                                            <strong t-esc="(log.description or log.action.replace('_', ' ').title())"/>
                                            <span>
                                                <t t-if="log.platform_user_id"><t t-esc="log.platform_user_id.name"/> · </t>
                                                <t t-esc="log.action.replace('_', ' ').title()"/>
                                            </span>
                                        </div>
                                        <time t-field="log.create_date"/>
                                    </article>
                                </div>
                                <div t-else="" class="fl-empty-state fl-empty-compact">
                                    <span class="fa fa-history"/>
                                    <h3>No recent activity</h3>
                                    <p>Platform events will appear here as users work.</p>
                                </div>
                            </div>
                        </section>
                    </main>

                    <footer class="fl-platform-footer">
                        <span>FlexSys Platform · 19.0.1.7.0</span><span>Stable core. Flexible experience.</span>
                    </footer>
                </div>
            </div>
        </t>
    </template>

    <template id="workspace_page" name="FlexSys Workspace">
        <t t-call="web.layout">
            <div class="fl-platform-page fl-workspace-page">
                <div class="fl-shell">
                    <header class="fl-launcher-header">
                        <div class="fl-brand"><a href="/flexsys" class="fl-brand-mark">FS</a><div><h1 t-esc="application.name"/><p>Workspace</p></div></div>
                        <div class="fl-user-context"><form action="/flexsys/search" method="get" class="fl-header-search" role="search"><span class="fa fa-search"/><input type="search" name="q" minlength="2" maxlength="100" placeholder="Search..." aria-label="Search FlexSys"/></form><span class="fl-user-pill" t-esc="platform_user.name"/><a href="/flexsys" class="fl-btn fl-btn-ghost"><span class="fa fa-th-large"/> Mission Control</a></div>
                    </header>
                    <t t-call="flexsys_platform.command_palette"/>
                    <main>
                        <section class="fl-workspace-hero">
                            <div><span class="fl-eyebrow"><span t-attf-class="fa #{application.icon}"/> Workspace</span><h2 t-esc="application.workspace_title or application.name"/><p t-esc="application.workspace_subtitle or application.summary"/></div>
                            <div class="fl-workspace-context"><strong t-esc="company.name"/><span t-if="branch" t-esc="branch.name"/><span t-else="">All branches</span></div>
                        </section>
                        <section t-if="metric_items" class="fl-kpi-grid">
                            <article t-foreach="metric_items" t-as="item" class="fl-kpi-card">
                                <span t-attf-class="fl-kpi-icon fa #{item.icon}"/><div><span t-esc="item.title"/><strong t-esc="metrics.get(item.metric_key or item.code, 0)"/></div>
                            </article>
                        </section>
                        <section t-if="action_items">
                            <div class="fl-section-head"><div><h2>Quick actions</h2><p>Common tasks for this workspace.</p></div></div>
                            <div class="fl-action-grid"><a t-foreach="action_items" t-as="item" t-att-href="item.action_url or '#'" class="fl-action-card"><span t-attf-class="fa #{item.icon}"/><div><strong t-esc="item.title"/><small t-esc="item.description"/></div><span class="fa fa-arrow-right"/></a></div>
                        </section>
                        <section t-if="widget_items">
                            <div class="fl-section-head"><div><h2>Overview</h2><p>Live workspace information.</p></div></div>
                            <div class="fl-widget-grid"><article t-foreach="widget_items" t-as="item" t-attf-class="fl-panel fl-widget fl-widget-#{item.size}"><div class="fl-widget-head"><span t-attf-class="fa #{item.icon}"/><div><h3 t-esc="item.title"/><p t-esc="item.description"/></div></div><div class="fl-widget-value" t-esc="metrics.get(item.metric_key or item.code, '—')"/></article></div>
                        </section>
                    </main>
                    <footer class="fl-platform-footer"><span>FlexSys Platform · 19.0.1.7.0</span><span>Workspace Framework v1.0</span></footer>
                </div>
            </div>
        </t>
    </template>

    <template id="search_page" name="FlexSys Universal Search">
        <t t-call="web.layout">
            <div class="fl-platform-page fl-search-page">
                <div class="fl-shell fl-launcher">
                    <header class="fl-launcher-header">
                        <div class="fl-brand"><a href="/flexsys" class="fl-brand-mark">FS</a><div><h1>Search</h1><p>Universal Search</p></div></div>
                        <div class="fl-user-context"><span class="fl-user-pill" t-esc="platform_user.name"/><a href="/flexsys" class="fl-btn fl-btn-ghost"><span class="fa fa-th-large"/> Mission Control</a></div>
                    </header>
                    <t t-call="flexsys_platform.command_palette"/>
                    <main>
                        <section class="fl-search-hero">
                            <div><span class="fl-eyebrow"><span class="fa fa-search"/> Universal Search</span><h2>Find anything in FlexSys</h2><p><t t-esc="company.name"/><t t-if="branch"> · <t t-esc="branch.name"/></t></p></div>
                            <form action="/flexsys/search" method="get" class="fl-global-search fl-global-search-page" role="search">
                                <span class="fa fa-search" aria-hidden="true"/>
                                <input type="search" name="q" t-att-value="query" minlength="2" maxlength="100" autofocus="autofocus" placeholder="Search orders, customers and more..." aria-label="Search FlexSys"/>
                                <button type="submit" class="fl-btn fl-btn-primary">Search</button>
                            </form>
                        </section>
                        <section class="fl-search-results">
                            <div class="fl-section-head"><div><h2>Results</h2><p t-if="query"><t t-esc="len(results)"/> result(s) for “<t t-esc="query"/>”</p><p t-else="">Enter at least two characters to search.</p></div></div>
                            <div t-if="results" class="fl-search-list">
                                <a t-foreach="results" t-as="result" t-att-href="result['url']" class="fl-search-result">
                                    <span t-attf-class="fl-search-result-icon fa #{result['icon']}"/>
                                    <div class="fl-search-result-content"><div class="fl-search-result-title"><strong t-esc="result['title']"/><span class="fl-status-chip" t-esc="result['type']"/></div><p t-if="result['subtitle']" t-esc="result['subtitle']"/><small><span t-attf-class="fa #{result['application_icon']}"/> <t t-esc="result['application_name']"/></small></div>
                                    <span class="fa fa-arrow-right fl-app-arrow"/>
                                </a>
                            </div>
                            <div t-elif="query" class="fl-empty-state"><span class="fa fa-search"/><h3>No results found</h3><p>Try an order number, customer name, or mobile number.</p></div>
                            <div t-else="" class="fl-empty-state"><span class="fa fa-keyboard"/><h3>Start typing</h3><p>Universal Search respects your permissions, company, and branch context.</p></div>
                        </section>
                    </main>
                    <footer class="fl-platform-footer"><span>FlexSys Platform · 19.0.1.7.0</span><span>Universal Search v1.0</span></footer>
                </div>
            </div>
        </t>
    </template>

</odoo>
