# FLPOS 19.0.6.3.0

This release removes Receipt Studio completely and keeps FLPOS focused on receipt identity settings and POS session closing reports.

Upgrade cleanup removes legacy Receipt Studio actions, views, permissions, fields, and database tables.
